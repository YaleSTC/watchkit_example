//
//  connectAPI.swift
//  WatchKit Example
//
//  Created by Shanelle Roman on 10/7/16.
//  Copyright © 2016 Yale SDMP. All rights reserved.
//

import UIKit
import Foundation



class connectAPI: NSObject  {
   /*
    public func getRoutes( )->Void
    {
        // configure URL session
        let session = URLSession(configuration: URLSessionConfiguration.default)
        
        // create NSURL request
        let urlString = "https://transloc-api-1-2.p.mashape.com/routes.json?agencies=yale&geo_area=41.3%2C-72.9%7C5000"
        let url = URL(fileURLWithPath: urlString)
        var request = NSMutableURLRequest(url: url)
        
        // set method type and body
        request.httpMethod = "GET"
        request.setValue("application/json", forHTTPHeaderField: "accept")
        request.setValue("tbZ4YpVZyMmshj9yNjoXCsqXr3k9p1R36r5jsnH541Ejy6Qk6y", forHTTPHeaderField: "X-Mashape-Key")
        
        let task = session.dataTask(with: request as URLRequest, completionHandler: {(data, response, error) in
            
            // create the returning JSON data object
            if (data == nil)
            {
                // NO IDEA WHAT TO DO NEXT
                completionnil
            }
            else
            {
               let responseJSON =  try? JSONSerialization.jsonObject(with: data!, options: JSONSerialization.ReadingOptions.mutableContainers)
                return responseJSON

            }
            
        });
        
        task.resume()

 
    } */ 
    

    
    
    
    

}
